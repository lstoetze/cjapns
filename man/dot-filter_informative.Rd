% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amce.R
\name{.filter_informative}
\alias{.filter_informative}
\title{Filter rows to informative tasks for a level pair (Definition 3 in paper)}
\usage{
.filter_informative(attr_vals, task_key, tq, tp, profile_vals = NULL)
}
\arguments{
\item{attr_vals}{Attribute column from the data (vector).}

\item{task_key}{Character vector identifying each task (respondent x task).}

\item{tq, tp}{The two levels being compared.}

\item{profile_vals}{Optional profile indicator vector (e.g. "a"/"b").}
}
\value{
Logical vector of length `length(attr_vals)`, TRUE for rows in
  informative tasks.
}
\description{
A task is informative for pair (tq, tp) if exactly one profile shows tq and
all J-1 remaining profiles show tp, or vice versa.
}
\details{
When `profile_vals` is provided the check compares the two profiles
explicitly (profile a vs profile b). Otherwise falls back to counting levels
within the task, which generalises to J > 2.
}
\keyword{internal}
